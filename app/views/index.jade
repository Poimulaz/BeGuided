extends layout

block content
  h1= title
  p Welcome to #{title}
  if (user)
    h2= (user.name + "  " + user.firstname)
  each user in users
    li= (user.name + "  " + user.firstname + "  " + user.password)